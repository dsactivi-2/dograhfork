# Allowed seams

Only the files in `SEAM_MAP.md` may be edited under `api/`.
Each edited line must contain `CUSTOM-SEAM`.

If an upstream merge drops a marker, restore it from `custom/backup-*`
before pushing.
